import React from 'react';
import { Text } from 'react-native-paper';

const AccountView = () => {
    return (
        <Text>
            my account
        </Text>
    );
};

export default AccountView;
